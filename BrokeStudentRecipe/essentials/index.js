import images from "./images"
import theme, { COLORS, SIZES, FONTS } from "./theme"
import mockData from "./mockData"

export {
    images,
    theme,
    COLORS, 
    SIZES, 
    FONTS,
    mockData
}